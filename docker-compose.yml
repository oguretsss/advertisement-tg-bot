services:
  repostbot:
    build: .
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - CHANNEL_ID=${CHANNEL_ID}
      - DB_PATH=/app/data/advertisements.db
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
